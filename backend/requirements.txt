Django>=5.0,<6.0
djangorestframework
django-environ
django-cors-headers
cloudinary
django-cloudinary-storage
django-bunny
Pillow

# Production-only (Postgres driver + WSGI server). Not required for local SQLite development.
psycopg2-binary
gunicorn
